{namespace klog}

{template .index}
    {@param posts: list<[id: int, title: string, text: string]>}
    {call klog.header /}
        <h1>Hello world!</h1>

        {if length($posts) > 0}
            <ol>
            {foreach $post in $posts}
                <li>{$post.title}</li>
            {/foreach}
            </ol>
        {else}
            <p>No posts!</p>
        {/if}
    {call klog.footer /}
{/template}

